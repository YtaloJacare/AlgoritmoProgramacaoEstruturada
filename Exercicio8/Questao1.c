#include<stdio.h>
#include<math.h>

main(){

int idade, MaiorI, MenorI;
char Ava;

printf("[Avaliação - Black Cinemas Corporation]");

do{
    nt++;
    fflush(stdin);

    do{
        printf("\nInforme sua Idade:");
        scanf("%d", &idade);
        
        MaiorI > MenorI;
        if(idade > MenorI && idade == MaiorI){
            idade =  MaiorI;
        } else {
            idade = MenorI;
        }
        fflush(stdin);

        if(idade <= 0){
            printf("\nIdade Invalidade. \n Digite sua Idade:");
        }
    }while (idade <= 0);


}while(idade != -1);
}